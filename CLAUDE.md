# CLAUDE.md

Правила для AI-агентов, работающих с этим репозиторием. Читаются при старте каждой сессии.

<!-- Rendered from setup-agent-harness template. Update via /harness-update. -->

## Общие принципы

- Общение с пользователем — RU. Код, идентификаторы, коммиты — английский.
- Документация — `docs/` (индекс: `docs/README.md`).
- Таск-трекер — `docs/tasks/` (индекс: `docs/tasks/INDEX.md`, формат: `docs/tasks/README.md`).
- Не создавать документацию/файлы без явной необходимости или запроса пользователя.
- Проект: **case-file-site (Дело № 15.07)** — HTML/CSS/JS статика + Python-скрипты (фото-обработка), GitHub Pages.

## Session-start ritual (первое сообщение новой сессии)

**До ответа на первый вопрос пользователя** в новой сессии:

1. Прочитать `docs/tasks/INDEX.md`.
2. Собрать список задач со `status: in_progress` (id + title).
3. Одной строкой сообщить пользователю: «В работе: T-X (title), T-Y (title). Продолжаем что-то из них или новая задача?»
4. Дальше — по реакции:
   - **Пользователь называет ID** → открыть таск-файл, прочитать `## План` и `## Лог` (последняя запись — где остановились), продолжать от неё.
   - **Пользователь описывает новую задачу** → следовать триажу в `.claude/skills/task-add.md`.
   - **Пользователь молчит про in_progress** → считать, что задача новая, следовать триажу; но помнить про in_progress'ы (могут стать блокером/зависимостью).

Если `in_progress`-таски пусты — начать без ритуала, сразу к вопросу пользователя.

Ritual — **однократный** на сессию. Не повторять с каждым сообщением.

## Session-end ritual (пользователь заканчивает сессию)

**Триггеры** (только явные — implicit-детект «молчания» ненадёжен):

- Пользователь сказал: «на сегодня всё», «пауза», «закончили», «переключаемся», «спасибо, до завтра», «go afk» и подобное.
- Пользователь явно попросил зафиксировать состояние («что мы делали?», «обнови таск»).

**Что делаю до прощания:**

1. Определить `in_progress`-таски, над которыми работали в этой сессии.
2. Для каждой — обновить `## Лог`:
   ```
   - YYYY-MM-DD: пауза. Остановились на: <шаг>. Следующий шаг: <что делать>. Блокеры: <есть/нет + описание>.
   ```
3. Commit + push обновлений (по правилам git-автоматизации).
4. Одной строкой сообщить: «Сохранил состояние в T-X (`## Лог`). До связи.»

Если в сессии не было работы над задачами (только чат / investigation) — ritual не нужен, просто попрощаться.

## Перед началом работы (для каждой новой задачи в сессии)

1. Прочитать `docs/tasks/INDEX.md` — есть ли уже задача под текущий запрос?
2. Если есть — открыть таск-файл, следовать `acceptance_criteria`, обновить `status: in_progress`.
3. Если нет и работа нетривиальная — предложить пользователю создать задачу через `/task-add`.
4. Прочитать релевантные `docs/*.md` (см. таблицу «Как читать» в `docs/README.md`).

## Во время работы

- Отмечать прогресс во встроенном `TaskList` (in-session tracker) — это отдельное от `docs/tasks/`, для оперативных шагов внутри одной задачи.
- Не менять то, что не относится к текущей задаче. Сторонние правки — отдельным таском.

## После изменений в исходниках

**Обязательный чек после каждого набора изменений:**

1. **Обновить связанные документы.** Найти в `docs/*.md` все места, где упоминаются изменённые файлы/классы/методы (по имени или ссылке типа `file:line`), синхронизировать:
   - Ссылки на строки в исходниках (номера могли уехать).
   - Сигнатуры функций/методов / поля моделей/DTO / переходы состояний / роли.
   - Таблицы endpoints/сервисов/DTO/модулей.
   - При структурном изменении — обновить `docs/11-known-gaps.md` (или аналог).

2. **Обновить таск-файл.** В связанном таске (`docs/tasks/T-XXX-*.md`):
   - Добавить запись в `## Лог` с датой и кратким описанием.
   - Если работа завершена — вызвать `/task-done` (или обновить frontmatter вручную: `status: done`, `updated: <дата>`).

3. **Обновить `docs/tasks/INDEX.md`** — статус и `updated` дата в таблице.

Если сомневаешься, какие docs затронуты — вызвать `/docs-sync`. Если нужно проверить целостность трекера — `/task-sync`.

## Формат задач

См. `docs/tasks/README.md`. Кратко:

```yaml
---
id: T-XXX
title: Короткий заголовок
status: pending | in_progress | blocked | done | cancelled
priority: low | medium | high
created: YYYY-MM-DD
updated: YYYY-MM-DD
related_code:
  - path/to/file.<ext>
related_docs:
  - docs/XX-name.md
tags: [backend, api, security, ...]
---
```

Тело: `## Контекст`, `## Acceptance criteria`, `## План`, `## Лог`.

## Slash-команды

| Команда | Действие | Git |
|---------|----------|-----|
| `/task-add` | Создать новый таск | commit (без push) |
| `/task-done` | Закрыть таск | тесты → commit → push |
| `/task-sync` | Аудит трекера | нет |
| `/docs-sync` | Актуализация docs | commit → push (если правки) |
| `/wheel-check` | До новой реализации: проверить готовые аналоги в проекте / фреймворке / либах | нет |
| `/mid-retro` | Пауза внутри задачи для само-осмотра (скоуп, ошибки, follow-up'ы) | нет |
| `/self-review` | После `task-done`: ревью diff'а по 5 категориям (акцент — улучшения меня) | иногда micro-fix commit |
| `/pre-flight` | Между дизайном/анализом и первым Edit/Write в исходниках — 3 вопроса: assumptions, risks, reversibility. Для non-trivial и high-stakes зон | нет |
| `/harness-update` | Sync `.claude/skills/` проекта с последними templates из harness-репо. Explicit-only (пользователь запрашивает вручную). Показывает diff-summary + spawns commit + push | commit + push |
| `/consolidate` | Периодический sweep закрытых задач на кросс-задачные паттерны в E-категории self-review. Auto каждые 10 done-задач или explicit | commit (state + tasks) → push |

## Проактивные триггеры (без явной команды)

Skills вызываются автоматически в перечисленных ситуациях. Не ждать команды пользователя — самому решать по условию:

| Условие | Skill | Что делать |
|---------|-------|-----------|
| После блока Edit/Write в исходниках | `docs-sync` | Проверить упоминания изменённых файлов в `docs/*.md`, предложить правки |
| Изменённый файл в `related_code` открытой задачи | ручное обновление таска | Запись в `## Лог`, обновить `updated`; при закрытых acceptance → предложить `task-done` |
| По ходу другой работы замечена проблема вне текущего скоупа | `task-add` в Auto-mode | Завести таск с фильтром (см. ниже), одной строкой сообщить пользователю |
| Задача переведена в `done` | `docs-sync` | Убедиться что все `related_docs` синхронизированы |
| Массовое переименование/удаление в исходниках | `task-sync` | Проверить `related_code` во всех открытых задачах |
| Пользователь спрашивает «что там в задачах» | без skill | Прочитать `docs/tasks/INDEX.md`, ответить |
| Перед новой реализацией (>30 строк / новая зависимость / «менеджер»/«фабрика»/«регистри») | `wheel-check` в Auto-mode | Проверить готовые аналоги; вердикт в одну строку в отчёт |
| 3+ подряд Edit/Write в исходниках без паузы, ИЛИ 2+ раз подряд правлю свои же ошибки (компил/линтер/тест), ИЛИ одна проблема сопротивляется >15 мин | `mid-retro` в Auto-mode | Ответить пользователю по 5 вопросам (что сделал / скоуп / ошибки / follow-up / надо ли переспросить) |
| Сразу после успешного `task-done` (после commit+push) | `self-review` в Auto-mode | Ревью diff'а по 5 категориям, обязательный акцент на пункт E (улучшения меня самого) |
| Долгая dev-команда выбивается за 2× baseline (см. «Долгие команды» ниже) без выхлопа в stdout | без skill | Диагностика: `ps`, stdout, внешние зависимости (Docker, порты). Эскалировать пользователю раньше чем ставить очередной wakeup |
| Первое сообщение новой сессии | без skill | Session-start ritual (см. секцию выше) — прочитать `INDEX.md`, объявить `in_progress`-таски, спросить продолжение или новая задача |
| Пользователь явно завершает сессию («на сегодня всё», «пауза», «до завтра», «afk») | без skill | Session-end ritual (см. секцию выше) — обновить `## Лог` `in_progress`-тасков записью «остановились на / следующий шаг / блокеры», commit + push, попрощаться |
| После brainstorming/systematic-debugging для non-trivial задачи, перед первым Edit/Write в исходниках | `pre-flight` в Auto-mode | Answer 3 questions: assumptions/risks/reversibility. Пауза до кодинга |
| Задача трогает high-stakes зону (DB migration, security-config, deletion, prod-настройки, cross-cutting breaking API) | `pre-flight` в Auto-mode + external review в `task-done` | `pre-flight` обязателен даже для formally-trivial. При закрытии — Часть 3 Verification gate (user-check и/или subagent code-reviewer, см. `task-done.md`) |
| После task-done, если с прошлой consolidation закрылось ≥10 задач | `consolidate` в Auto-mode | Одной строкой предложить пользователю запустить `/consolidate`; не запускать без confirm'а |
| Баг / упавший тест / неожиданное поведение | `superpowers:systematic-debugging` | Не пытаться сразу фиксить — гипотеза → эксперимент → вывод. Особенно важно если проблема сопротивляется дольше ~10 минут |
| Новая фича с ясным acceptance criteria | `superpowers:test-driven-development` | **RED→GREEN обязателен.** До реализации — написать провальные тесты по каждому AC и **прогнать их, увидеть FAIL** (сохранить в лог или ответе). Только потом — реализация до зелёного. |
| Многошаговый plan есть (`docs/superpowers/plans/*.md`) — начинаем прогон | `superpowers:executing-plans` | Дисциплинированный прогон с checkpoint'ами, а не «зачитал и побежал» |

## Проактивное заведение задач (Auto-mode для `task-add`)

Когда по ходу работы над одной задачей замечена проблема или идея **вне текущего скоупа** — агент имеет право сам завести таск.

**Фильтр «три условия»** — заводить только если все три верны:
1. Проблема реальная (не косметика).
2. Формулируется `## Контекст` в 2-3 строки и минимум один пункт `## Acceptance criteria`.
3. Не относится к текущей задаче и растворится без записи.

**Правила исполнения:**
- Заведение ≤ 30 секунд: минимальный скелет и обратно к текущей работе.
- Обязательный тег: `tech-debt` (техдолг в проекте) или `meta` (улучшение CLAUDE.md / skills / docs).
- Приоритет `low` по умолчанию; `medium` — для явно опасных; `high` — с согласия пользователя.

**Правило прозрачности:**

В конце ответа, где сам завёл таск, отдельной строкой:

> Попутно завёл **T-XXX** (`tech-debt`|`meta`, `low`|`medium`): _краткое описание_. Отменить — скажи.

## Git-автоматизация

Стоячий мандат от пользователя: коммитить и пушить завершённую работу автоматически, **без запроса подтверждения**. Работает в ветке `main`.

### Когда коммитить и пушить

| Событие | Действие |
|---------|----------|
| Skill `task-done` успешно завершился | тесты → commit → **push** |
| Skill `docs-sync` применил правки | commit → **push** |
| Skill `task-add` создал таск | commit → **без push** |
| Задача вручную в `done` | напомнить про `task-done` |
| Быстрый фикс без задачи по запросу | commit → push |

### Формат commit-сообщения

Conventional Commits + ссылка на таск:

```
<type>(<scope>): <короткое описание>

<опциональное тело>

Refs: docs/tasks/T-XXX-*.md
```

- **type:** `feat` | `fix` | `refactor` | `docs` | `test` | `chore` | `perf` | `style`.
- **scope:** `T-XXX` или подсистема.

### Safety-guards (обязательные)

1. **Stage только именованные пути.** Никогда `git add -A` / `git add .`.
2. **Секреты.** Перед commit — `git diff --cached | grep -Ei '(password|secret|token|api[_-]?key|jwt|credential)\s*[:=]'`. При совпадении — блок commit, показать пользователю.
3. **Крупные бинарники** (>1MB) — блок, спросить.
4. **Опасные файлы:** `.env*`, `*.pem`, `*.key`, `id_rsa*`, `credentials*.json`, `*.log` — блок.
5. **Никогда** `--no-verify`, `--no-gpg-sign`, `--amend` без запроса, `-i`.
6. **`git push` только `origin main` без force.**
7. **Тесты**: для статического сайта тест-команды нет — проверить открытие в браузере вручную перед commit'ом если изменения затрагивают UI.
8. **Чужие изменения:** stage'ить только своё; при сомнениях — показать пользователю `git status --short`.

### Что коммитим совместно

Изменения в исходниках + связанные `docs/*.md` + запись в таске = **один commit**.

## Superpowers integration

Наши 7 skills — оркестраторы. `superpowers:*` — процессные инструменты внутри. Карта wire-points:

| # | Триггер | Наш slot | Superpowers-skill |
|---|---------|----------|-------------------|
| 1 | `/task-add` non-trivial задачи | шаг перед созданием файла | `superpowers:brainstorming` |
| 2 | Многошаговая задача, spec есть | между `task-add` и кодингом | `superpowers:writing-plans` |
| 3 | Перед flip'ом status→done | шаг в `task-done` | `superpowers:verification-before-completion` |
| 4 | Баг / упавший тест / неожиданное поведение | триггер в таблице «Проактивные триггеры» выше | `superpowers:systematic-debugging` |
| 5 | Перед прогоном 5 категорий self-review | шаг 2 в `self-review` | `superpowers:receiving-code-review` (внутренний диалог) |
| 6 | Новая фича с acceptance criteria | триггер в таблице | `superpowers:test-driven-development` |
| 7 | Прогон существующего plan'а | триггер в таблице | `superpowers:executing-plans` |

**Правила:**
- Разграничение обязанностей: наши skills — учёт (файл-таск, INDEX, git), superpowers — процессная дисциплина.
- Не создавать новые skills для того, что superpowers уже покрывает.
- Не переписывать superpowers — только звать.
- Артефакты superpowers'ов кладутся в `docs/superpowers/specs/` и `docs/superpowers/plans/`.

## Стилевые ограничения

- RU в текстах; в коде и идентификаторах — английский.
- Комментарии — только про «почему», не про «что».
- Не добавлять фичи/абстракции сверх запрошенного.
- Диффы минимальные и точечные.

## Запуск и проверки

- Сборка: `n/a — статический сайт; python3 process_photos.py для обработки фото`.
- Тесты: `n/a — статика; проверка: открыть index.html в браузере + Lighthouse mobile`.

## Долгие команды — эвристика ожидания

Baseline'ы для этого проекта (с прогретым кэшем):

| Команда | Ожидаемое время | Порог тревоги |
|---------|-----------------|--------------|
| `python3 process_photos.py` (все фото) | ~60s на фото / ~5-10 min для пакета | >20 min |
| `python3 generate_manifest.py` | ~5s | >30s |
| `bash process_media.sh` | ~5-15 min (зависит от кол-ва фото) | >30 min |
| `python3 -m http.server 8000` до READY | мгновенно | >5s |
| Lighthouse CLI audit | ~30s | >2 min |

Правило: если команда идёт **>2× ожидаемого** без единой новой строки в stdout — **не ставить ещё один wakeup**, а прогнать быструю диагностику.

### Универсальное правило для long-running команд

**Единственно правильный паттерн:**

1. **Любая long-running команда** запускается через `Bash(run_in_background=true)` с редиректом в файл: `<cmd> > /tmp/<name>.log 2>&1`.
2. **Wait — только по task-notification.** Дождался `completed` → отдельным вызовом `tail -N /tmp/<name>.log`.
3. **Не polling'ать через sleep.**

## Что не делать

- Не создавать PR без запроса.
- Не менять конфигурацию (package.json, etc.) без запроса.
- Не удалять существующие задачи — только `status: cancelled`.
- Не устанавливать/обновлять зависимости без запроса.
- Не force-пушить.
- Не пушить в другие ветки/remote.
- **Не коммитить абсолютные пути и machine-specific настройки.**
- **Не добавлять бэкенд, API-ключи, аналитику, трекеры** — сайт должен оставаться статичным.
- **Не хотлинковать фото** с внешних сервисов.
- **Не выдумывать личный контент** сверх написанного в `docs/06-personal-content.md`.

## Проектные особенности (из анализа проекта)

### Жёсткие ограничения

- **Без бэкенда.** Только статика: HTML/CSS/JS. Никаких серверов, БД, API-ключей.
- **Mobile-first.** Основной сценарий — телефон после скана QR. Ширина 380px — главный viewport при проверке. Десктоп 1280px — бонус.
- **Хостинг — GitHub Pages.** Все пути относительные (`./css/style.css`, `./photos/photos.json`). Абсолютные пути сломают деплой.
- **Приватность.** Все фото лежат локально в `/photos`. Никаких хотлинков на Instagram или другие сервисы. Никакой аналитики (GA, Metrica, pixel).
- **Производительность.** Lazy loading + WebP + превью-размеры (thumb 400px). Первый экран быстро на мобильном интернете.
- **Внимание:** Google Fonts подключены через CDN (`index.html:10-14`) — потенциальное нарушение приватности; обсудить с владельцем.

### Порядок чтения документации перед работой

1. `docs/01-concept.md` — концепция, тон, эмоциональная драматургия
2. `docs/02-design-system.md` — палитра, типографика, компоненты, сигнатурный элемент
3. `docs/03-content-structure.md` — секции сайта, тексты, плейсхолдеры
4. `docs/04-tech-hosting.md` — стек, ограничения, деплой
5. `docs/05-photos.md` — манифест фотографий
6. `docs/06-personal-content.md` — личные тексты (НЕ выдумывать сверх написанного!)

### Контент — правила

- Имя получателя: **Юля** (обращения: Юленька, Солнце, Бусенька — в зависимости от раздела)
- Личные тексты — **только** из `docs/06-personal-content.md`. Ничего не выдумывать.
- `[TODO]`-пункты в `06-personal-content.md` (дата VR-вечера, формулировка «сложный период») — уточнить у владельца до публикации. → T-001
- Тон: каждая шутка — комплимент в обёртке. Если фраза смешная, но не тёплая — переписать. К финалу ирония уступает искренности.

### Рабочий процесс

1. Скелет всех секций с плейсхолдерами → затем дизайн-система и красная нить.
2. Контент из `photos/photos.json` и `content.json` — не хардкодить в HTML.
3. Проверять скриншотами на 380px (мобайл) и 1280px (десктоп) после значимых изменений.
4. `prefers-reduced-motion` должен отключать анимации нити и параллакс.

### Definition of Done проекта

- Сайт полностью работает с `file://` и с GitHub Pages (относительные пути!)
- Все секции из `docs/03-content-structure.md` реализованы
- Красная нить анимируется по скроллу и связывает улики
- Звуковые эффекты (штамп, бумага) работают после первого касания и имеют кнопку mute
- Lighthouse mobile: Performance ≥ 85, Accessibility ≥ 90
- Ни одного захардкоженного личного текста вне `photos.json` / `content.json`
